import Default from '../../layout/default'
import HomeBanner from '~/components/blocks/MainBanner/MainBanner'

const Home = () => {
  return (
    <Default>
      <HomeBanner />
    </Default>
  )
}

export default Home
